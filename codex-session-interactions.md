# مستند تعاملات با Codex - آزمایش TDD سبد خرید


## وضعیت نهایی قابل استناد

- اجرای کامل تست‌ها: **36 تست، 0 خطا، 0 شکست**.
- JaCoCo برای کلاس `ShoppingCart`: **100% Line Coverage، 100% Branch Coverage و 100% Method Coverage**.
- PIT برای کلاس `ShoppingCart`: **100% Line Coverage (36/36)، 100% Mutation Coverage (19/19) و 100% Test Strength (19/19)**.
- پوشش کل پروژه لزوماً 100% نیست، زیرا `Main` و `Item` در رفتار سبد خرید استفاده نمی‌شوند. معیار گزارش اصلی این آزمایش کلاس `ShoppingCart` است.

## تعامل 1 - خواندن و استخراج الزامات آزمایش

- **درخواست دانشجو:** مطالعهٔ دقیق صورت‌مسئله و ارائهٔ مسیر انجام آزمایش.
- **پاسخ/تحلیل Codex:** الزام چرخهٔ Red-Green-Refactor، سه باگ مستقل، قابلیت `updateItemPrice`، قابلیت تجاری دوم، JaCoCo، PIT، تاریخچهٔ Git و مستندسازی تعامل‌ها استخراج شد.
- **تصمیم/اقدام مشاهده‌شده:** اجرای پروژه به‌صورت مرحله‌ای و ثبت شواهد انتخاب شد.

## تعامل 2 - انتخاب GitHub و محیط توسعه

- **درخواست دانشجو:** انجام پروژه با GitHub و IntelliJ IDEA.
- **پاسخ/تحلیل Codex:** پیشنهاد شد مخزن GitHub عمومی باشد، پروژهٔ پایه به‌عنوان upstream حفظ شود و JDK 17 در IntelliJ تنظیم شود.
- **تصمیم/اقدام مشاهده‌شده:** IntelliJ و Maven برای اجرای تست‌ها و گزارش‌ها استفاده شد.

## تعامل 3 - تحلیل Baseline

- **درخواست دانشجو:** شناخت ساختار و رفتار پروژهٔ اولیه.
- **پاسخ/تحلیل Codex:** مشخص شد `ShoppingCart` وضعیت را در `HashMap<String, Double>` نگه می‌دارد؛ افزودن نام تکراری قیمت را جایگزین می‌کند؛ حذف کالای موجود مقدار `true` و حذف ناموجود مقدار `false` برمی‌گرداند.
- **تصمیم/اقدام مشاهده‌شده:** تست‌های پایه بدون تغییر اجرا و شواهد اولیه ذخیره شد.

## تعامل 4 - پیکربندی Maven، JaCoCo و PIT

- **درخواست دانشجو:** ایجاد پیکربندی قابل تکرار برای تست و سنجش کیفیت.
- **پاسخ/تحلیل Codex:** فایل `pom.xml` با JUnit 5، Java 17، مسیرهای غیر استاندارد `src` و `Test`، JaCoCo و PIT تنظیم شد.
- **تصمیم/اقدام مشاهده‌شده:** ابزارهای تست و گزارش در Maven فعال شدند.

## تعامل 5 - رفع پیکربندی اولیهٔ PIT

- **درخواست دانشجو:** بررسی خطای PIT که به علت الگوی بسیار گسترده رخ داده بود.
- **پاسخ/تحلیل Codex:** علت، الگوی `*` برای کلاس هدف بود که PIT را به بررسی وابستگی‌های خودش می‌کشاند. هدف به `ShoppingCart` محدود شد.
- **تصمیم/اقدام مشاهده‌شده:** گزارش PIT با کلاس هدف صحیح اجرا شد.

## تعامل 6 - باگ اول: دقت اعشاری مجموع

- **درخواست دانشجو:** تحلیل تست مجموع `0.1 + 0.2`.
- **پاسخ/تحلیل Codex:** تست Red نشان داد مقدار واقعی `0.30000000000000004` است، در حالی که انتظار رفتار مالی `0.30` بود.
- **تصمیم/اقدام مشاهده‌شده:** محاسبهٔ مجموع با `BigDecimal.valueOf` و گرد کردن `HALF_UP` به دو رقم اعشار اصلاح شد.
- **شواهد:** تصویر `2.png` شکست Red این سناریو را نشان می‌دهد.

## تعامل 7 - دقت مبلغ پس از تخفیف

- **درخواست دانشجو:** بررسی تست تخفیف برای مجموع `100.01`.
- **پاسخ/تحلیل Codex:** تست Red مقدار `90.009` را در برابر مقدار مورد انتظار `90.01` آشکار کرد.
- **تصمیم/اقدام مشاهده‌شده:** محاسبهٔ تخفیف نیز با `BigDecimal` و گرد کردن دو رقم اعشار پایدار شد.
- **شواهد:** تصویر `3.png` شکست Red این سناریو را نشان می‌دهد.

## تعامل 8 - مرز قانون تخفیف

- **درخواست دانشجو:** بررسی رفتار مجموع دقیقاً 100.
- **پاسخ/تحلیل Codex:** تعارض بین متن اولیهٔ راهنما و یک تست پایه تشخیص داده شد و توصیه شد تصمیم مدرس/دستیار آموزشی مستند شود.
- **تصمیم/اقدام مشاهده‌شده:** دانشجو اعلام کرد که موضوع با TA حل شده است؛ پیاده‌سازی و تست نهایی باید با همان تصمیم مستند سازگار باشند.
- **شواهد:** تصویر `4.png` نمونهٔ شکست Red در مرز تخفیف است.

## تعامل 9 - تست‌های `updateItemPrice`

- **درخواست دانشجو:** بررسی مجموعه تست‌های قابلیت تغییر قیمت.
- **پاسخ/تحلیل Codex:** سناریوهای تغییر موفق، ثابت ماندن تعداد، تغییر مجموع و تخفیف، نام ناموجود، نام تهی/خالی، قیمت صفر/منفی، مقدار اعشاری و مقادیر نامتناهی بررسی شد.
- **تصمیم/اقدام مشاهده‌شده:** قرارداد نهایی این شد که کالای ناموجود بدون تغییر وضعیت نادیده گرفته شود و ورودی‌های نامعتبر با `IllegalArgumentException` رد شوند.

## تعامل 10 - پیاده‌سازی `updateItemPrice`

- **درخواست دانشجو:** بررسی پیاده‌سازی Green.
- **پاسخ/تحلیل Codex:** امضای متد به `updateItemPrice(String, double)` تغییر کرد؛ اعتبار نام و قیمت پیش از هر تغییر بررسی شد؛ در صورت نبودن کالا، وضعیت سبد ثابت می‌ماند.
- **تصمیم/اقدام مشاهده‌شده:** تست‌های قابلیت جدید پاس شدند و رفتارهای قبلی حفظ شد.

## تعامل 11 - قابلیت تجاری دوم: حداقل و حداکثر مبلغ خرید

**پرسش دانشجو:** برای مجموع سبد حداقل و حداکثر تعریف کرده‌ام
 تست‌ها را تحلیل کن.

**پاسخ Codex:** قرارداد `validateCheckout()` تعریف شد: مجموع پایه قبل از تخفیف باید در بازه‌ی بسته‌ی `50.00` تا `10000.00` باشد و در خارج از بازه `IllegalStateException` بدون تغییر وضعیت سبد پرتاب شود.

**ارزیابی/تصمیم دانشجو:** تست‌های مرز پایین و بالا، سبد خالی، مقادیر خارج از بازه و بررسی مجموع قبل از تخفیف افزوده شدند. `8.png` شواهد Red (نبود متد) و `9.png` اجرای Green را ثبت می‌کنند.

## تعامل 12 - تست‌های قابلیت محدودهٔ مبلغ

- **درخواست دانشجو:**  بررسی تست‌های Red/Green برای `validateCheckout`.
- **پاسخ/تحلیل Codex:** سناریوهای حداقل مجاز، حداکثر مجاز، سبد خالی، کمتر از حداقل، بیشتر از حداکثر و اعتبارسنجی مجموع پایه پیش از تخفیف بررسی شدند.
- **تصمیم/اقدام مشاهده‌شده:** هر دو مرز مجاز و حالت‌های خارج از بازه بدون تغییر حالت سبد رد شدند.
- **شواهد:** تصویر `8.png` مرحلهٔ Red (نبودن متد) و تصویر `9.png` اجرای موفق تست‌ها را نشان می‌دهد.

## تعامل 13 - JaCoCo و PIT نهایی

**پرسش دانشجو:** گزارش‌های نهایی را اجرا و تفسیر کن.

**پاسخ Codex:** تفاوت Coverage و Mutation Score توضیح داده شد و لزوم اجرای PIT با مجموعه‌ی تست کاملاً سبز بیان شد.

**ارزیابی/تصمیم دانشجو:**

- `10.png`: پوشش کلی پروژه را نشان می‌دهد؛ این مقدار شامل کلاس‌های استفاده‌نشده‌ی `Item` و `Main` نیز هست.
- `11.png`: پوشش کلاس `ShoppingCart` را 100% برای خط، branch و method نشان می‌دهد.
- `12.png`: گزارش PIT نهایی را با 100% Line Coverage، 100% Mutation Coverage و 100% Test Strength نشان می‌دهد.

## تعامل 14 - بازبینی نهایی وضعیت پروژه

**پرسش دانشجو:** چه موارد دیگری برای مطابقت با صورت مسئله باقی مانده است؟

**پاسخ Codex:** نیاز به commit کردن تغییرات Green، نگه‌داری گزارش فارسی، مستندسازی تعاملات واقعی، بررسی وضعیت Hamgit/GitHub و مستندسازی تناقض مرز ۱۰۰ مطرح شد.

**ارزیابی/تصمیم دانشجو:** این فایل به عنوان بخشی از مستند تعاملات ایجاد شد و باید همراه با گزارش نهایی فارسی تکمیل و بازبینی شود.
## جمع‌بندی ابزارهای تست پیشرفته

- **Parameterized Tests:** در تست‌های ورودی ناموجود، مجموع زیر آستانه و ورودی‌های نامعتبر `updateItemPrice` استفاده شده‌اند.
- **Exception Tests:** برای نام نامعتبر، قیمت صفر/منفی/نامتناهی و مجموع خارج از بازهٔ checkout استفاده شده‌اند.
- **Scenario Tests:** شامل افزودن، تغییر قیمت، حذف، تخفیف، نام تکراری، حداقل/حداکثر مبلغ و حفظ اتمیک بودن وضعیت هستند.
- **Mocking:** استفاده نشده است، زیرا پروژه هیچ وابستگی خارجی، پایگاه داده، سرویس شبکه یا درگاه پرداخت ندارد که نیازمند جداسازی با Mock باشد.

## فهرست تصاویر شواهد

| فایل | محتوای قابل استفاده در گزارش |
| --- | --- |
| `F:\\swlav\\3\\1.png` | اجرای اولیهٔ 4 تست پایه |
| `F:\\swlav\\3\\2.png` | شکست Red برای دقت مجموع اعشاری |
| `F:\\swlav\\3\\3.png` | شکست Red برای گرد کردن تخفیف |
| `F:\\swlav\\3\\4.png` | شکست Red برای مرز تخفیف |
| `F:\\swlav\\3\\5.png` | گزارش JaCoCo اولیه |
| `F:\\swlav\\3\\6.png` | گزارش PIT اولیه |
| `F:\\swlav\\3\\7.png` | گزارش میانی PIT |
| `F:\\swlav\\3\\8.png` | مرحلهٔ Red قابلیت محدودیت مجموع |
| `F:\\swlav\\3\\9.png` | اجرای موفق کامل تست‌ها |
| `F:\\swlav\\3\\10.png` | گزارش کلی JaCoCo پس از توسعه |
| `F:\\swlav\\3\\11.png` | گزارش 100% JaCoCo برای `ShoppingCart` |
| `F:\\swlav\\3\\12.png` | گزارش 100% PIT نهایی |
